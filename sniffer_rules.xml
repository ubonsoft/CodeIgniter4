<?xml version="1.0"?>
<ruleset name="CodeIgniter 4 Standard">

    <!-- Default to all system files if nothing specified -->
    <file>./system</file>

    <exclude-pattern>*/tests/*</exclude-pattern>
    <exclude-pattern>*/writable/*</exclude-pattern>
    <exclude-pattern>*/user_guide_src/*</exclude-pattern>
    <exclude-pattern>*/public/*</exclude-pattern>

    <!--<arg name="report" value="summary"/>-->
    <arg name="colors"/>

    <!-- User PSR1 as our basis, and add on after that -->
    <rule ref="Squiz">
    </rule>
</ruleset>
